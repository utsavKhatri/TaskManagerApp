# App demo video

## File

- **Path:** [`demo/app-demo-2026-04-02.mp4`](./demo/app-demo-2026-04-02.mp4)
- **Recorded:** 2026-04-02 (device screen recording; original export name included a device/session suffix)

## What it shows

This MP4 is a screen recording of **TaskManagerApp** running on a device. It illustrates the current React Native UI and typical flows for the features described in the main README: tasks, categories, filtering, ordering, and related interactions (for example swipe actions and list behavior where applicable).

Use it to onboard collaborators, verify UI behavior against a known build, or include in presentations without running the app locally.

## How to view

- **Locally:** open `docs/demo/app-demo-2026-04-02.mp4` with any video player (QuickTime, VLC, etc.).
- **In repository browsers:** many hosts link or embed MP4 previews; if preview is unavailable, download the file and play it locally.

## Size

The file is on the order of tens of megabytes; clone or fetch may take longer on slow connections.
